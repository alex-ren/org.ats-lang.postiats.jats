

var g := 3

fun main0 (x) = let
  val y = add (1, x)
  val z = mul (y, g)
  val () = createEvt ("a")
in
  z
end

fun foo (x) = let
  val () = createEvt ("foo")
in
  3
end

val z = main0 (1) 
val _ = foo (z)




